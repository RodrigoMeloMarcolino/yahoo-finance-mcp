startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/RodrigoMeloMarcolino/yahoo-finance-mcp",
        "yahoo-finance-mcp"
      ]
    })
